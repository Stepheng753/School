package Lectures;

public class FinalStudyGuide
{
    /**
     * API - Application Program Interface
     * JDK - Java Development Kit
     * JRE - Java Runtime Environment
     * IDE - Integrated Development Environment
     *
     *
     * String Class
     *  charAt - returns letter at specified index of String
     *
     *  compareTo - returns an int, based off alphabetical order.
     *      - If same, return 0
     *      - If a then c, return -2 (1-3)
     *      - If c then a, return 2 (3-1)
     *
     *  concat - puts two strings together
     *
     *  equals - returned True/False whether the strings are equal
     *
     *  indexOf - returns index of specified Index at letter of String
     *
     *  isBlank - return True if nothing within String. It can have whitespaces
     *
     *  isEmpty -returns True if nothing within String, cannot have Whitespaces
     *
     *  length - returns length of String
     *
     *  split - splits a string by each regex and becomes an array
     *
     *  substring- returns a subString of a String with parameters of begin/end index
     *
     *  trim - trims off whitespace at the end
     *
     *  valueOf - returns the valueOf an int, char, charArray as a String
     *
     *
     *
     * Double Class
     *  compareTo - returns an int, based off size
     *      - firstNumber.compareTo(secondNumber)
     *      - If same, return 0
     *      - If firstNumber is smaller than secondNumber, return -1
     *      - If firstNumber is bigger than secondNumber, return 1
     *
     *  equals - returns True/False whether or not the doubles are equal
     *
     *  doubleValue - returns an Integer, Byte, Number as a double
     *  floatValue - returns as a float
     *  intValue - returns as a int
     *
     *  parseDouble - converts a String to double
     *
     *  valueOf - returns an int, char, charArray as a double
     *
     *
     *
     * Integer Class
     *  compareTo - returns an int, based off size
     *      - firstNumber.compareTo(secondNumber)
     *      - If same, return 0
     *      - If firstNumber is smaller than secondNumber, return -1
     *      - If firstNumber is bigger than secondNumber, return 1
     *
     *  doubleValue - returns an Integer, Byte, Number as a double
     *  floatValue - returns as a float
     *  intValue - returns as a integer
     *
     *  parseInt - converts a String to int
     *
     *  valueOf - returns an int, char, charArray as an int
     *
     *
     *
     * Character Class
     *  - charValue - returns a Character as a char
     *
     *  - compareTo - returns an int, based off alphabetical order.
     *      - If same, return 0
     *      - If a then c, return -2 (1-3)
     *      - If c then a, return 2 (3-1)
     *
     *
     *
     *  Inheritance Hierarchy
     *      - Object
     *      - Throwable
     *      - Exception
     *
     *  IOException - IO means Input/Output Exception - when can't find input file or output source
     *  IndexOutOfBoundsException - Exception when trying to index a list, String, array, etc
     *  ArrayIndexOutOfBoundsException - Exception when trying to index an array
     *  NullPointerException - Exception when trying to use an Object that is of null value
     *  StackOverflowError - Recursive, never ending loop
     *
     *  Method Overloading - Same method name, different parameters
     *  Method Overriding - Change the inside of the method based on Class
     *
     *  % - returns remainder of quotient
     *
     *  Constructor gets called when instantiating a class
     *  Default Constructor - when no Constructor has been implicitly made
     *  Multiple Constructors with diff Input Parameters - example of method Overloading
     *
     *  Try - Looks through this code
     *  Catch - Implements this code, if exception has been thrown
     *  Finally - Do after both try and catch
     *
     *  AutoBoxing - primitive to Wrapper
     *      - "Wraps the Box"
     *  Unboxing - "Wrapper to primitive
     *      - "Opens box to get primitive"
     *
     *  Inheritance - Extends
     *  Polymorphism - Extends, Method Overloading, Overriding
     *  Encapsulation - Hiding the variables as private, or putting it on another class
     *
     *  CompileTime Polymorphism is choosing what to do before running.
     *      - EX: Method Overloading
     *  Runtime Polymorphism is choosing what to do at runtime
     *
     *  Encapsulation - Information hiding.
     *      - EX: Variables and Methods
     *  Abstraction - Implementation hiding
     *      - EX: Inside of a method
     *
     *
     *
     *
     */

    public static void main(String[] args) {


    }
}
